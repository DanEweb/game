# Gray Survivor — 세션 핸드오프 (다른 데스크탑에서 이어가기)

> 마지막 갱신: 2026-08-14 · 현재 버전 **v6.155** (배치 6 완결 — 테크트리 리워크 4개 항목 전부)
>
> ⚠⚠⚠ **주석과 조건이 어긋나면 조건을 의심할 것 — 공통 테크 트리가 통째로 죽어 있었다** (v6.154)
> 강림 필터가 `tkey!==focusTree` 하나뿐이라 `common`까지 지웠다. 주석에는 *"무속성·무기·전용은 유지"* 라고 적혀 있었는데도.
> 강림은 항상 어떤 속성으로 정해지므로 **공통 테크 전부(연마·단련·속보·집중·재생·행운·거인 사냥꾼·불굴)가 등장한 적이 없다**.
> ⚠ 카드 화면을 눈으로 훑어서는 안 보인다 — **`__qaPool(6)`으로 풀을 직접 뽑아 볼 것**(이번에도 25번 뽑고서야 잡혔다).
> ⚠ 공통 트리가 처음 살아났으므로 **체감 파워가 오를 수 있다** — 상한 하향(연마 9→5 등)이 상쇄하는지 실플레이 확인 필요.
>
> ⚠⚠ **테크트리는 엔진을 키우는 곳이다** (v6.154 · 배치 6) — 축을 만들어 놓고 키울 카드가 없었다.
> 엔진 강화 5종: 연성(자원 회전) · 파장(범위) · 연격(타수) · 여운(각성 창) · 벼림(위력, 3티어).
> ⚠ 위력만 주면 v6.144의 실수를 반복한다 — **회전율·범위·타수·창으로 나눠 줄 것**.
> ⚠ 공통 트리는 레벨업마다 **2개만 무작위 허용**이라 새 노드를 넣어도 묻힌다(실측 16회 레벨업에 0회) — 자리 보장이 필요하다.
>
> ⚠⚠⚠ **행동 축을 만들면 '그 상황이 아예 안 오는 판'을 반드시 생각할 것 — 봉인 방지 하한** (v6.152)
> 관리자(적탄 요격)·공허술사(장판 위)·기술자(터렛 작동)가 실측에서 **0.00**이었다. 조건이 안 오면 전용기를 영영 못 쓴다.
> ⇒ 조건 미충족이어도 **만충 ~70초**로는 찬다(맞추면 5~15초). *"조건은 관리이지 봉인이 아니다"*(v6.140 원칙).
> ⚠ **대가를 두 번 물리지 말 것** — 조준 게이트(v6.140)를 행동 축에 그대로 두었더니 값이 30%로 깎여
> 세 직업이 사실상 0이었다. 행동 자체가 이미 조건이다.
>
> ⚠⚠ **D축(방어)은 직업 이름을 갖고, 없는 직업에겐 아예 없다** (v6.152 · `GUARD_NAME`)
> 사무라이 '받아넘기기' / 관리자 '요격 결재' / 백수 '흘리기' … **null이면 키도 버튼도 동작하지 않는다**.
> 장사·소환·관측이 정체성인 직업(관광객·지휘관·디버거…)에게 방어 버튼은 화면만 가린다.
>
> ⚠⚠⚠ **잡몹도 덤비기 전에 예비동작(0.32초)을 한다 — 근접 패링의 유일한 근거다** (v6.151)
> 예전엔 잡몹이 *닿으면 그냥 피해*라 근접 패링이 운이었다(탄막만 패링이 성립했다). 이제 몸을 뺐다가 달려든다.
> 판정(`e.windT`)과 그림이 같은 값을 쓴다 — **보이는 것과 판정이 어긋나면 그 순간 기술이 아니라 운이 된다**.
> ⚠ 여기에 `player.invuln<=0`을 걸면 **대시 무적 중엔 적이 덤비지 않아** 백수(저스트 회피)가 영원히 안 찬다(실측 0.00).
>
> ⚠⚠⚠ **근접 기력은 평타·처치로 차지 않는다 — 직업마다 다른 '행동'으로 찬다** (v6.150~v6.151 · 사용자 지시)
> 평타/처치는 *가만히 있어도 일어나는 일*이라 조건이 아니라 시간이었다. `MELEE_ENGINE[k].act`가 그 행동이다:
> **15개 엔진이 전부 다른 조건**이다: 패링(사무라이) · 저스트회피(백수) · 관통대시(창기병) · 포위탈출(낫) · 표적전환(그림자) · 빈사처형(사신) · 콤보(수도승) · 정박(선율가) · 저체력유지(철혈) · 자기소모(광인) · 포위유지(헬창) · 표적유지(결투가) · 위기(용사) · 구역(성기사) · 무조건(무명자).
> **새 근접 엔진을 만들면 `act`를 반드시 줄 것** — 없으면 그 직업은 R를 영영 못 쓴다. 충전은 `meleeActCharge(kind)` 한 곳으로 모인다.
> ⚠ `parry` 계열은 **하네스로 검증 불가**(하네스 성립률 5% / 사람 60%) — v6.145와 같다. 하네스 수치를 목표로 삼지 말 것.
> ⚠ 실측 함정 두 개: **`__qaGod()`는 받는 피해를 0으로 만들어 `hurt` 계열이 영원히 안 찬다** /
> 직업↔엔진 대응을 기억으로 쓰지 말 것(철혈은 `whirl`이 아니라 **`riposte`**) — `__qaState().eng`로 확인.
>
> ⚠⚠ **영역(피바다)은 플레이어 발밑에만 생긴다** (v6.150 · 사용자 지시) — 적이 죽은 자리가 아니다.
> *"그래야 영역의 의미가 있지"*. 영역의 값어치는 **적의 발을 묶는 자리**이고, 딜도 기력 배수도 아니다.
> ⚠ 이 변경으로 *영역 위 기력 ×1.85*가 **상시 조건**이 되어 만충 0.5초가 나왔다 → **배수 제거(1.0)**.
> **위치·조건을 바꾸면 그 조건에 얹혀 있던 배수를 반드시 다시 볼 것.**
>
> ⚠⚠ **무기가 판마다 바뀌는 직업(무작위 무기)은 '직업만 보는 축'을 준다** (v6.150 · `FREE_CLASS`)
> 수집가·글리치·최병우 3직업. ⚠ 충전을 **막는 방식은 계속 뚫린다**(무기별 충전 코드가 곳곳에 흩어져 있다) —
> 자체 값(`freeChg`)에 쌓고 **매 프레임 `mCharge`를 덮어쓰는** 방식이라야 출처가 몇 개든 결과가 하나다.
> ⚠ 새 '끌어오기/생성' 효과를 만들 때마다 물을 것: **그 결과물이 다시 자원이 되는가?**(수집가 R가 끌어온 오브로 자기증식했다 → `o.byTrove`)
>
> ⚠⚠⚠ **상한은 '모든 반복 회복이 그 함수를 지날 때만' 의미가 있다** (v6.149)
> 흡혈 상한(`healCapped`)이 있었는데 **성역·수확 궁극·신성 구역이 우회**하고 있었다.
> 특히 성역 회복은 **적 루프 안**에 있어 *적 1마리당* 초당 1 — 30마리면 초당 30이었다.
> ⇒ 새 회복을 만들면 **반복성인가**를 먼저 묻고, 반복이면 `healCapped`를 지나게 할 것(일회성 줍기는 예외).
> 현재 상한 **최대체력 2%/초**. ⚠ 상한이 최대체력 비례라 **탱킹 빌드일수록 상한도 커진다**는 점을 기억할 것.
>
> ⚠⚠⚠ **감소·회피·재생에는 반드시 하한/상한이 있어야 한다 — 곱연산 스택은 임계점에서 절벽처럼 무적이 된다** (v6.148)
> `dmgTaken *= 0.9x`가 **89곳**인데 하한이 없었다(10개면 0.43, 20개면 0.19). 회피는 개별 출처만 막혀 있었고,
> 재생은 초당 받는 피해를 넘었다. 실측: 10분 시점에 풀빌드가 30초에 **47 피해**(맨몸 400의 1/8.5).
> ⇒ 하한·상한은 **`playerHit` 한 곳**에서 건다. 출처마다 걸면 새 출처가 생길 때마다 또 뚫린다.
> ⚠ 난이도 계수를 고칠 땐 **`powerScale=1.0`(성장 전)에서 기존과 같아지게** 설계할 것 — 초반을 건드리지 않고 후반만 조인다.
>
> ⚠⚠⚠ **역방향 루프에서 배열이 줄면 프레임이 통째로 스킵된다 = 게임이 사실상 정지한다** (v6.148)
> `for (let i=enemies.length-1;...){ const e=enemies[i];` 에서 루프 안의 처치·폭발·연쇄가 **여러 마리를 한 번에** 지우면
> `enemies[i]`가 undefined가 되고, 워치독이 그 프레임을 버린다(벤치에서 **킬 0 · 적 92마리 누적**으로 관측).
> v6.147의 *"원인 미규명"* 크래시가 이것이었다. **23곳 전수 가드 완료** — 새 루프를 쓸 땐 `if (!e) continue;`를 같이 넣을 것.
> 검출: `node scanloops.mjs`
>
> ⚠⚠ **'항상 참인 조건'은 조건이 아니다 — 판정을 만들면 반드시 반대 조건도 재 볼 것** (v6.148 패링)
> '완벽 패링'을 두 번 틀렸다: ①맞는 순간 예고 여부 → 7/7 ②누른 순간 예고 여부 → **마구 눌러도 100%**.
> 무리 속에선 접촉 예고가 상시라 갈리지 않는다. ③ **무엇을 받아쳤는지**(적탄·보스)로 바꿔서야 갈렸다.
>
> ⚠ **후반 난이도는 `__qaTime(초)`로 잰다** (v6.148 신설) — 기존 벤치는 전부 12~20초라 **10분 뒤 세상을 재본 적이 없었다**.
> 같이 쓰는 훅: `__qaPeril(n)` · `__qaPower(lv,tech,star,equip)` · `__qaState().taken`(정확 누적 피해).
> ⚠ **체력 샘플링으로 피해를 재지 말 것** — 재생이 사이를 메워 *재생 빌드가 더 안전해진 것처럼* 나온다. `taken`을 쓸 것.
>
> ⚠⚠⚠ **v6.141 이전의 벤치 수치를 근거로 쓰지 말 것 — 하네스가 배경 탭에서 조작을 빠뜨리고 있었다** (v6.147)
> `__qaAuto`가 `setInterval(…,100)`이었는데 **숨김 탭에서 타이머는 1초로 클램프**된다 →
> 대시(1.4초 간격)가 **14초**가 되어 12초 벤치에서 **한 번도 안 나갔다**. 게임 심은 rAF를 살려 두는데 **조작만 죽어 있었다**.
> 지금은 rAF 구동으로 고쳤다(같은 조건에서 12초 대시 0회 → 9회).
> ⚠ **새 QA 코드에서 `setTimeout`으로 대기하지 말 것** — rAF 기반 `sleep`을 쓸 것.
> ⚠ `window.__gameErr`는 **한 번 켜지면 안 꺼진다** — 런 시작 시 직접 지우지 않으면 **옛날 에러를 지금 에러로 오독**한다(실제로 그랬다).
>
> ⚠⚠⚠ **같은 무기를 들어도 직업마다 화력원이 정반대다 — 무기 단위 조정은 반드시 직업별로 검증할 것** (v6.147)
> 위성 5직업을 분해했더니 **관리자는 위성이 화력의 100%**(위성 0 → **0킬**)인데 **룬 기사는 3%**였다.
> 관리자는 스킬 4종이 전부 유틸이라 위성 말고 때리는 수단이 없다.
> **위성을 5% 깎으면 관리자만 5% 약해지고 룬 기사는 아무 일도 안 일어난다.**
> ⚠ 분해 절차: `__qaForce({satDmg:0.0001})`처럼 **한 출처만 죽이고** 3회 평균으로 비교. ±10%는 노이즈.
>
> ⚠⚠ **인덱스 기본값에 `||`를 쓰지 말 것 — 그리고 한 곳을 고쳤으면 같은 패턴을 전수조사할 것** (v6.147)
> `SAT_ORBIT[player.satOrbit||1]`은 **기본값 0(표준 궤도)일 때 확장 궤도의 값을 읽는다**(`0||1`은 1).
> v6.143에서 이걸 **한 곳만** 고쳤고, 나머지 두 곳이 남아 **표준 궤도의 피해 감소·요격 반경이 한 번도 작동한 적이 없었다.**
>
> ⚠ **새 타격원을 만들면 ① 보스 루프 ② 런 리셋 ③ 상한**, 셋을 같이 볼 것 (v6.147 사출·회수)
> ① `enemies`만 짜면 보스전에서 무효(v6.126) ② 리셋에서 안 비우면 다음 런이 **궤도가 빈 채로** 시작
> ③ 관통에 상한이 없으면 밀집에서 **쓸어담는 기술**이 된다 — 위성의 약점은 밀집이 아니라 **단일 대상**이었다
>
> (이전 v6.144)
> ⚠⚠⚠ **전용기의 가치는 '위력'이 아니라 '판을 뒤집는 조작'이다** (v6.144 · 사용자 지적)
> 근접 설계는 *평타 느리고 약함 + 붙는 위험 → 그 대가로 전용기(손맛·이점)* 였는데,
> 평타도 전용기도 좋아져 **위험만 사라지고 보상은 두 배**가 됐다. 여기에 Q·1~4 스킬이 곱해지면 붕괴한다.
> **위력을 올리고 싶어질 때마다 타수·범위·연출·조작가치로 돌려라.**
>
> ⚠⚠ **영역(피바다)에는 폭주 경로가 둘 있다 — 새 영역 효과를 만들 때 반드시 확인할 것** (v6.144)
> ① **자기증식**: 영역으로 죽은 적이 또 영역을 낳으면 스스로 번진다 → `e.byZone`으로 차단
> ② **원격 생성**: 광역기로 화면 끝의 적을 죽여도 거기 영역이 깔린다 → 발밑 190px 제한
> ③ 겹칠 때 `dps = max(dps, power)`로 갱신하면 밀집에서 초당딜이 계속 오른다 → 넓이·수명만 갱신할 것
>
> ⚠⚠⚠ **한 판으로 '성공'을 선언하지 말 것 — `__qaBenchAvg(names, 킬초, 생존초, 3)`을 쓸 것** (v6.141→v6.142)
> v6.141에서 *"룬 기사 108→79, 위성 하향 성공"* 이라고 적었는데 **3회 평균은 102**였다(79가 이상치).
> 그 잘못된 판정 위에서 다음 조정을 결정할 뻔했다. 관광객 107·지휘관 130도 전부 이상치였다.
> **실패는 한 판으로도 보이지만 성공은 반복으로만 보인다.**
> ⚠ 직업마다 편차 폭이 다르다 — 파일럿 41~46(±6%)인데 **궁수 31~99·관광객 33~90(±50%)**.
> 둘 다 **이동 의존형**이라 하네스의 자동 이동 패턴에 흔들린다. 편차가 큰 직업은 **5회 이상** 잴 것.
>
> ⚠⚠⚠ **조정 전에 '이 직업의 킬이 어디서 나오는가'를 먼저 분해할 것** (v6.143)
> v6.141에서 *"킬 순위를 무기로 묶으면 위성 5직업이 상위 독식 → 무기가 갈랐다"* 고 진단하고 두 버전을 조정했는데,
> 실제로 분해해 보니 **룬 기사는 위성이 킬의 4%, 스킬·궁극·R이 90%**였다(정상 114 / 위성0 109 / 스킬없이 11).
> 묶어 보면 그럴듯한 패턴이 늘 보인다 — **패턴은 가설이지 원인이 아니다.**
> 분해 방법: `__qaForce({satDmg:0.0001})`처럼 **한 출처만 죽이고** 킬을 비교한다.
> ⚠ 직업마다 출처 비중이 다르다 — 닌자는 무기 강화가 먹혔는데(46→76) 룬 기사는 안 먹힌다.
>
> ⚠ **이 벤치는 사실상 '스킬 회전율 테스트'다** — `__qaAuto`가 스킬(1~4)·궁극(Q)·R를 쿨마다 누른다.
> 무기 성능을 재려면 `__qaAuto` 없이(이동만) 재거나 `__qaDummy` 단일 대상 벤치를 쓸 것.
>
> ⚠ **`__qaBenchAll(names, 킬초, 생존초)`** — 런이 끝나면 `showIdle()`로 타이틀에 돌아가므로
> **한 페이지에서 19직업 연속** 측정이 된다(직업마다 새로고침하면 20분이 든다). 잠긴 직업은 `__qaUnlock('*')` 먼저.
>
> ⚠⚠⚠ **직업별 설정을 호출부에서 넘기지 말 것 — 쓰는 함수 안에서 읽어라** (v6.140)
> `RANGED_CLASS[k].mult`(직업별 '차는 조건')를 호출부에서 넘겼더니, 넘기는 곳이 두 군데뿐이라
> **11직업이 조용히 1.0 고정**이었다. v6.120 `scytheBoost`(읽기 삭제), v6.137 `surgeT`(계열이 안 읽음)에 이은
> **세 번째 같은 사고**다. 공통점은 전부 *"값은 있는데 그 값을 쓰는 자리가 빠졌다"* 이고,
> 셋 다 **실측으로만** 잡혔다 — 코드를 읽어서는 안 보인다.
>
> ⚠ **원거리 자원은 조준에 묶여 있다** (v6.140) — `aimChargeMult() = 0.30 + aim×0.90`.
> 원거리 전용기의 **대가**가 이것이다(근접은 붙어야 차는 것이 대가). 하한 0.30은 의도된 것 —
> 물량에 파묻힌 순간 R가 영영 막히면 '관리'가 아니라 '봉인'이 된다.
> 실측: 궁수 만충까지 조준 0 → 18.1초 / 조준 1 → 5.0초.
>
> ⚠ **원거리는 `RANGED_CLASS` 표에서 직업으로 분기한다 — 무기를 쪼개지 말 것** (v6.131·v6.139)
> 19직업 전원이 전용 자원(이름·색·차는 조건)과 전용 R를 갖는다. 새 원거리 직업을 추가하면
> ① `RANGED_CLASS`에 `{res, sig, col, mult}` ② 소비기 분기에 `sig` 케이스 — 두 곳을 채울 것.
> 무기를 쪼개면 카드 풀·성장무기·전향 판정이 전부 배로 늘어난다.
>
> ⚠⚠ **새 축을 만들면 `__qaAxes(8)`로 계열별 반영을 확인할 것** (v6.138)
> 축을 강제로 켜고 단일 대상 DPS가 변하는지 잰다. 배율 1.00 = 그 직업에게 아무 일도 안 한다.
> 현재 기준선: 사무라이 `surgeT` 1.62 / `meleeBoost` 1.50 / `rushT` 1.77, 궁수 `aim` 2.58 / `surgeT` 1.23 / 과부하 1.41.
> ⚠ 노이즈 ±15% — **'역방향' 판정은 재측정 없이 결론 내지 말 것**.
> ⚠ 축은 **짝을 이루는 값까지 같이** 켤 것 (`odT`만 켜고 `odPower`=0이면 '과부하 무효'라는 가짜 결과가 나온다).
> ⚠ 잠긴 직업 검증은 `__qaUnlock('*')`.
>
> ⚠⚠⚠ **`audit.mjs`가 못 잡는 죽은 스탯이 있다 — "계열별로 안 읽히는" 경우** (v6.137)
> `surgeT`(소비 직후 3초 각성 창)는 `meleeAutoMult()` **한 곳에서만** 읽혔다. 원거리 8직업은 R를 쓰고
> `각성 3초` 문구까지 띄우면서 **보상이 0**이었다. 스탯 자체는 '읽히고' 있으니 감사 스크립트는 통과한다.
> **새 축을 만들면 `근접/원거리/하이브리드 전부에서 읽히는가`를 따로 확인할 것.**
>
> ⚠⚠ **카드가 떠 있으면 `KeyR`은 소비기가 아니라 리롤이다 — `__qaCards()`를 먼저 부를 것** (v6.137)
> 레벨업 카드 = `state==='levelup'` = 게임 정지. 이 상태로 재서 *"궁수 R이 아무 반응 없음"* 이라는
> **가짜 결론이 두 번** 나왔다(실제로는 정상, 69→105 dps). R·스킬 검증 전 `await __qaCards()`.
>
> ⚠⚠⚠ **측정 도구가 스탯을 건드리면, 그 스탯에 기대는 직업은 조용히 거짓말을 한다** (v6.136)
> `__qaGod()`는 원래 `maxHp=999999`였다. 그런데 헬창은 피해가 `1 + maxHp×0.00025`로 곱해진다 →
> **피해 251배**. 더미 실측 dps가 **1686**(사무라이 4의 400배)으로 찍혀서야 발견했고,
> **그때까지의 헬창 벤치 수치는 전부 무효**였다. 지금은 `player.qaGod`로 **받는 피해를 0**으로 만든다.
> **하네스가 건드리는 값(hp·maxHp·speed)에 성능이 걸린 직업이 또 있는지 의심할 것.**
>
> ⚠⚠ **런 시작은 2단계다 — `__qaStart(직업명)`을 쓸 것** (v6.136)
> `직업 카드 → 런 계약 카드`. 계약을 안 고르면 게임이 **멈춘 채**이고, 그 상태로 재면 값이 조용히 0으로 나온다
> (실제로 '정지 화면 8초'를 재고 "평타 0회"를 얻었다). `__qaStart`가 계약까지 자동으로 고른다.
> ⚠ 레벨업 카드가 뜨면 게임이 멈춘다 → **벽시계 시간 ≠ 게임 시간**.
> **주기·DPS 측정은 반드시 `__qaDummy` 벤치**(레벨업이 없다)에서 할 것.
>
> ⚠ **평타 공속은 `__qaState().autoN`으로 잰다** (v6.136) — 이 계측이 없어서 v6.135에서 *체감으로* +12%를 정했고,
> 그건 사용자가 말한 문제를 전혀 건드리지 못했다. **주기를 만질 땐 값으로 확인할 것** (현재 근접 평타 0.94초/회)
>
> ⚠ **탈것(`drawMountVec`)**: 창기병 전용이 아니라 **범용 함수**로 만들어 뒀다 — `player.mountKind`에
> `'horse'|'armored'|'wyvern'|'skeletal'|'phoenix'` 중 하나를 넣으면 어떤 직업이든 탈 수 있다.
> 새 탈것을 추가할 땐 이 함수 안에 분기 하나만 붙이면 되고, 전직 카드의 `fx`에서 `p.mountKind`를 세우면 된다.
> ⚠ **모션 언어**: 호(arc) 잔상 = 베기 / 직선 궤적 = 찌르기. 창 계열에 호 잔상을 쓰면 찌르는 것처럼 안 보인다
>
> ⚠⚠ **통합 리팩터링은 '읽기'를 떨어뜨린다** (v6.115 → v6.120에서 발견)
> 10종 분기를 하나로 합치면서 `player.scytheBoost` **읽는 쪽을 지웠다**. 쓰기 11건이 그대로 남아
> 사신·철혈의 "낫 피해 +N%"가 전부 무효인 채로 5개 버전이 흘렀다.
> **분기를 합칠 땐 지워지는 코드가 참조하던 전역을 먼저 세고, 통합본에 다시 연결할 것.**
> 배포 전 `scratchpad/audit.mjs`(쓰기만 있고 읽기 없는 스탯 검출)를 돌리면 잡힌다.
>
> ⚠ **근접 엔진 = 직업 고유** (v6.121). `ENGINE_OWNER`에 주인이 적혀 있고 **레벨업 카드 풀에 나오지 않는다**.
> 새 근접 엔진을 만들면 ① `WEAPONS` ② `MELEE_ENGINE`(평타 리듬·기력 조건·R 시그니처) ③ `MELEE_WEAPONS`
> ④ `ENGINE_OWNER` ⑤ 직업의 `weapon:` — 다섯 군데를 모두 채울 것. 하나라도 빠지면 주인 없는 엔진이 된다.
>
> ⚠ **배포 전 필수: `node audit.mjs`** — 쓰기만 있고 읽기가 없는 플레이어 스탯을 검출한다.
> v6.77에서 6종, v6.120에서 `scytheBoost` 회귀, v6.122에서 `catSmall`·`spendFocus`·`frenzyN`을 이걸로 잡았다.
> 결과가 **0건이 아니면 배포하지 말 것**. (투사체 필드와 이름이 겹치는 것은 스크립트의 `FALSE_POSITIVE`에 고정돼 있다)
>
> ⚠⚠ **새 소비기 시그니처는 `spendHitBosses()`를 반드시 같이 부를 것** (v6.126)
> `enemies` 루프만 짜면 **보스전에서 통째로 무효**가 된다. 실제로 거합·무영난무·백팔연타·마왕 처단·파쇄 돌격
> 5종이 보스 피해 0인 채로 배포돼 있었다(사무라이 단일 9dps). `friendlyBlast`(=`boom`)는 보스를 포함하므로 그건 안전.
>
> ⚠⚠ **벤치는 `__qaAuto(true)`로 실전 조작을 켜고 잰다** (v6.127)
> 기존 벤치는 *가만히 서서 평타만 치는* 조건이라 ① 이동이 정체성인 직업(창기병·백수)과
> ② **스킬(수동 Digit1~4)·궁극(Q)** 이 통째로 빠져 있었다. `__qaAuto`는 이동·대시·스킬·궁극·자원·패링을 자동으로 굴린다.
> 끌 때는 `__qaAuto(false)` — 방향키가 눌린 채로 남으면 다음 측정이 오염된다.
>
> ⚠⚠ **벤치는 반드시 두 축으로 잰다 — 밀집 + 단일 대상** (v6.125에서 확인)
> `__qaFlood(55)` 밀집 벤치만 보면 전 직업이 59~67로 균일해 보이는데, 같은 시점에 단일 대상 DPS는 **9~55, 6배 격차**였다.
> 밀집만 재고 있었으면 **소비기가 보스를 세지 않는 버그**(근접 R이 1:1에서 항상 헛방)를 영영 못 봤다.
> · 단일: `__qaDummy()` → `await __qaDps(15)` → `{dealt, sec, dps}`
> · 하네스 주의: 보스 키는 `BOSS_TYPES` 키를 쓸 것(`'ad'`는 패턴명) / 더미 거리 62px(위성·근접 사거리 안) /
>   측정 중 `enemies`를 비우지 않으면 무작위·최근접 대상 무기가 잡몹을 때려 값이 오염된다
>
> ⚠ **`__qaState()`는 JSON 문자열을 반환한다** — `JSON.parse(__qaState())`로 받을 것.
> `cls·weapons·heat·mCharge·gore·kills·enemies·zones·hp`를 노출하므로 벤치·감사에서 **값으로** 확인할 수 있다.
>
> 🔜 **다음 작업 = 배치 7 (무기 3층 정리 — 성물 37종·PGW 111 고유화·재료 드랍)**
> (참고) 이전 표기: 배치 4 (마법·사제·상인 — 속성 연계 / 가호 / 골드가 곧 탄약)**
> ⚠ 착수 전에 **그 직업들의 킬 출처를 먼저 분해**할 것 (v6.143·v6.147에서 두 번 확인된 절차)
> ⚠ 배치 5(전 계열 벤치)는 **v6.147 하네스 수정본으로 전 직업 재측정**부터 시작한다
>
> 🎮 **위성 조작(v6.147)**: `E` 궤도 전환 · `F` 사출/회수(모바일은 궤도 버튼 왼쪽 '사출' 버튼).
> 던지면 그 위성이 궤도에서 빠진다(요격·방어 감소) → 돌아올 때 **꿰뚫은 수만큼 과부하**가 찬다.
>
> ⚠⚠ **근접 설계 원칙 (v6.115 확정 — 이걸 어겨서 20버전을 되돌렸다)**
> **평타에 시그니처를 넣지 말 것.** 평타 = 빠르고 약한 평범한 근접 공격이고 본업은 **기력을 쌓는 것**이다.
> 클래스 정체성(거합·연참·혈풍륜·수확의 밤…)은 **R 소비기에서만** 나온다.
> 이 원칙을 어기면 ① 자동사냥 근접이 되고 ② 소비기가 평타의 상위호환이라 쓸 이유가 사라지고
> ③ 그걸 맞추려 평타 주기를 2.8초까지 늘리는 기괴한 숫자가 나온다 (v6.93~v6.113에서 실제로 일어났다).
> 신규 근접 엔진은 `MELEE_ENGINE` 표에 ⑴평타 리듬 ⑵기력이 빨리 차는 조건 ⑶R 시그니처, 이 셋만 정의하면 된다.
> ⚠ 소비기 다단 히트는 `queueTicks(n, iv, fn)` (프레임 큐)로. **`setTimeout`은 배경 탭에서 1초로 클램프되어 쓸 수 없다**
>
> ⚠ **조작 추가 규칙**: 새 조작은 **키 + 모바일 버튼을 한 세트**로 넣을 것. 키만 붙이고 끝낸 사고가 두 번 반복됐다
> (v6.85 궤도 전환 → v6.86에서 버튼 추가 / v6.107 자원 소비 → v6.108에서 버튼 추가)
> ⚠ **신규 엔진 작성 시**: 참조하는 전역 변수가 **실재하는지 먼저 확인**할 것 (`comboCount`라는 없는 변수를 쓸 뻔했다 — 실제는 `combo`)
> ⚠ **자원 만충 조건**: 도달 가능한지 계산할 것 (응혈은 `1-hp/maxHp`라 만충=사망이었다 → 30% 지점으로 정규화)
>
> 🔜 **(완료) 근접 엔진 2호 = 돌격병 '돌진 충격'**
> ⚠ 이건 무기 루프에 얹으면 안 된다. **대시 자체를 공격 판정으로 바꾸는 구조 변경**이라 `tryDash()`(약 9414행)를 먼저 읽을 것.
> 기존에 `dashBlast`(착지 폭발)·`dashRam`·`bloodRush`(dashExplosion)·대시 각인 4종이 이미 `tryDash`에 얽혀 있어
> 중복·이중 발동이 나기 쉽다. 설계: 대시 경로를 선분으로 잡아 그 위 적에게 1회 판정 + 경로에 짧은 충격대를 남긴다.
> 남은 엔진: 연격(수도승·백수·헬창) / 암습(닌자·그림자) / 회전 참격(철혈·광인) / 찌르기(창 계열).
> 역장은 **성기사·사제의 '신성 구역'** 으로 회수 예정. 엔진은 자원(E)·패링(D)과 한 세트 — **엔진을 먼저** 할 것
>
> 🔜 **진행 순서는 ROADMAP 최상단 「🗂 진행 순서 (Claude 정렬)」 표를 따를 것** (사용자 위임으로 9단계 확정)
> 1 근접 메리트 본구현 → 2 속성 마법 분리 → 3 무기 3층 정리(성물 37종·PGW 111 고유화·재료 드랍) →
> 4 칭호·치장템 → 5 업적·퀘스트 → 6 연출 대개편 → 7 보스 아종 → 8 측정 인프라 → 9 난이도 실측 조정
>
> (이전) v6.78 (밸런스 대개편 + 죽은 테크 6종 복구 + 모바일 시야 보정)
>
> ⚠ **테크 감사 필수**: 신규 테크·특성 추가 시 `scratchpad/audit.mjs`(쓰기만 하고 읽지 않는 스탯 검출)를 돌릴 것.
> v6.77에서 satDmg·multishotCh·explodeChance/Dmg·hitInvuln·reviveInvuln·weaponCap1 6종이 **한 번도 동작한 적 없음**을 이걸로 잡았다.
> ⚠ **역장(aura)은 procOnHit을 호출하지 않던 버그**가 있었다 — 새 지속피해 계열을 만들 땐 원소 발동 연결을 반드시 확인.
> ⚠ `W`·`H`는 v6.78부터 **CSS px이 아니라 월드 가시 범위** (모바일 카메라 축소 `VS`). CSS 크기가 필요하면 `cssW`·`cssH`.
>
> (이전) 현재 버전 **v6.76** (도트 스프라이트 렌더 + 공격 3박자 + **스프라이트 캐시로 모바일 렉 해결**(136마리 9ms→0.2ms) + 타격감 레이어(히트스톱·픽셀 파편·타격 스파크·피격 리액션) — 다음: **② 무기 직업군화 재편** (ROADMAP 📐 5항목) → ③전향 확장)
>
> ⚠ **성능 원칙**: 프레임마다 `getImageData`를 도는 코드를 개체 단위로 넣지 말 것. 도트 처리는 반드시 `dotPush(span,{key,ppu})`의 **키 캐시**를 통해서만 — 키에 넣을 수 없는 연속값(위치·각도·시간)은 양자화하고, 그래도 안 되면 도트화 제외.
> 성능 회귀 측정: `__qaFlood(150)` 후 `await __qaPerf(150)` → `{avg,max,enemies,sprites}`
>
> ⚠ 도트 렌더는 오프스크린에 그린 뒤 블릿하는 구조 — 그래서 `ctx`는 `const`가 아니라 **`let`**(잠시 스왑됨).
> 휴머노이드 실제 드로잉은 `drawHumanoidVec`, 몹은 `dotPush(span)` ~ `dotPop()` 사이만 픽셀화(이름·배지·오라는 밖에 둬야 가독성 유지).
> 검수 시 `localStorage.gs_dot='0'`으로 벡터 렌더 비교 가능. 공격 모션 검수는 `dist/bodytest.html?atk` (계열별 3박자 프레임 스트립)

## ⚡ 차기 세션 1순위 (v6.57 시점 — 진행 순서 사용자 확정: ①비주얼 ②무기 직업군화 ③전향 확장)
1. **비주얼 마스터플랜 계속** — 완료: 이펙트 1·2차(11속성 탄환·시전 마법진) + 몹 1차(스쿼시·기본 몹 생물화). 다음 순서:
   **몹 스킨별 리디자인·적탄 예고선 → 필드 보스 리디자인 → 기믹 보스·수문장(패턴·게임성 포함) → 맵 리얼화 → 일러스트 2단계(의상·얼굴)**.
   총체적 품질 기준은 ROADMAP 🎨 6번("실제 모바일게임 수준") — 공격모션 3박자 연출·타격감 튜닝 포함.
   전직 외형 대변화는 ROADMAP 🎨 1-b. 검수: dist/bodytest·efftest·mobtest.html + 스냅 서버 :8124
2. **무기 직업군화 전면 재편** — ROADMAP "📐 확정 설계" 5항목: 저희귀도 무기 직업군별 재제작(+전용 제한) / 크로스 장착 전수 차단(장비창 버그 포함) /
   근접 무기 테크 신설 + 전사군 근접 전용 카드(성도 원거리 투자 시 해제 — 전향 무기 v6.54 구현과 연동) / 근접·돌격 리스크 보상 / 각인·오라 외형 장식
3. **전향 무기 확장**: v6.54에서 4종 구현(검기 방출/총검 돌격/마나 블레이드/룬 폭충 — 성도 타 계열 별 1P+ 시 카드 등장). 도적·사제·상인 교차 + 전향 성장무기·유일무기는 ROADMAP ⇄ 섹션
4. **전직 실질 변화 확장 계속**: ⚙ 플래그 5종 (ricochet/shieldWall/dashRam/boltChain/dashCrit) — 나머지 직업 단계적
5. 검수 도구: dist/bodytest.html(전 직업 렌더 그리드) + 스냅 서버 :8124 + __qaWeapon/__qaSet/__qaGod 훅

## 📒 운영 규칙 (사용자 지시 — 상시)
- 사용자·Claude의 **모든 아이디어/개선안은 ROADMAP.md 💡 아이디어 로그에 기록** (제안자 구분), 채택되면 버전 로그로 이동
- **진행사항은 ROADMAP.md 버전 로그(vX.XX)로 정리**하고 HANDOFF.md를 매 세션 갱신

## 이 문서의 용도
다른 PC/새 세션에서 Claude Code로 작업을 이어갈 때, 아래 "이어가기 프롬프트"를 그대로 붙여넣으면 된다.

---

## 이어가기 프롬프트 (복사해서 새 세션 첫 메시지로)

```
친구의 흑백 뱀서라이크 게임 "Gray Survivor"를 디아블로/PoE급 RPG로 확장 중이야.
프로젝트: C:\Users\paku\Projects\gray-survivor (이 경로가 없으면 https://github.com/DanEweb/game 를 clone)
먼저 HANDOFF.md와 ROADMAP.md 전체를 읽고 현재 상태를 파악해.

핵심 규칙:
- 배포: npm run build → node build-single.mjs → dist/gray_survivor_single.html을
  "C:\Users\paku\OneDrive\Documents\카카오톡 받은 파일\gray_survivor_v3.html"로 복사 →
  git commit/push → GitHub Actions 성공 + https://daneweb.github.io/game/ 200 확인
- QA: node serve.mjs (포트 8123) → node make-qa.mjs → dist/qa.html 접속.
  ⚠ 숨김 탭은 setTimeout 1초 클램프로 게임 심이 멈춰 보임(0킬·보스 타이머 정지 = 코드 버그 아님) — make-qa.mjs의 MessageChannel 펌프 심이 이를 우회.
  훅: __qaGate(key|peril)/__qaBoss(frac)/__qaGod()/__qaState()/__qaSet(k,v)/__qaWeapon(key)
- ⚠ PowerShell Get-Content/-replace는 UTF-8 한글 파괴 — Edit 도구나 python/sed만 사용
- ⚠ buildStarTree IIFE는 RESONANCE/CLASSES/JOBVAR보다 먼저 실행 — 내부에서 직접 참조 금지(런타임 함수는 OK)
- ⚠ 신규 직업 추가 시 7곳 동기화: CLASSES, RESONANCE, RES_LOCAL, CGW_CLASS_GROUP RL, CS, SIGKEY/2/3, CGW_NAMES(+CSKIN, CLASS_COLORS, AWAKEN_BY_CLASS, drawHumanoid gear)
- 버전 로그는 ROADMAP.md에 vX.XX 형식으로 추가, 배포 후 메모리(gray-survivor-v2.md)도 갱신
- 사용자 지시 없이 커밋만 하고 끝내지 말 것 — 빌드→QA→배포→검증까지가 한 사이클
```

---

## 진행 단계 요약 (v6.9 → v6.49)

### 완성된 대형 시스템
- **관문 레이드**: 위험도 8~60, 15개 관문 전부 기믹 보스 + 다단 수문장 체인 + 체크포인트 이어하기 + QTE/줍기/엄폐 인터랙션. 최종 관문 60 = 회색 군주
- **직업**: 37종 전부 — 개별 소성단 + 시그니처 키스톤 3종(111종) + CSKIN + 고유 각성 + 전용 외형 프롭(무기가 실제로 보임) + 공격 스윙 모션
- **전직**: 1차(4택)/2차/3차 + 각성(3차 후에만) — 선택지마다: 스킬 각인(계열 6군×길 3 = 18종), 대시 각인(계열 성향 반영), 3차 상시 오라 3종, 티어별 실물 외형(견갑/문장석), 계승 변환 미리보기("▸ 열리는 것")
- **성도(영구)**: 별 웹 + 직업 대성단 + 길 성좌 18종의 대성단(심화/정점 — 정점=런에서 길 가지 3단계 무료) + 흐릿한 공명(4중첩) + 노터블 게이트 폐지(키스톤만 10P/30P)
- **승천반(런)**: 근본석+6방위+2·3차 링+전직 문장+각성 문장+분열 키스톤(낮/밤의 별 상호배타) + 전직 선택지별 전용 가지(JOBVAR)
- **무기 3층**: 직업 유일무기 CGW 111(0.01% 발견/도면 5%·5000G) / 직업 성장무기 PGW 111(제작·처치 성장·만렙 50·격 돌파 10/20/30/40·성능 균일 ARCH_BAL) / 전설 성장무기 4종(무명검+활/마도서/대검 — 상점 공방 폐지, 전용 직업 상인 이벤트 2%·4000G만) + 일반 무기 장비화(무기고 12칸)
- **레벨업 테크**: 11속성 × ~19노드 + 계열 슬롯제(무기 1 고정 / 전술·수호·보조·증폭·질풍 3 / 운명·금단 2, 🧩기억 확장 조각으로 확장) + 스킬 4슬롯(궁극은 Q 전용)
- **맵 13종**(배율 ×1~×14, 관문 보스가 필드 최종보스) / 시련 10종(별점 보상) / 의뢰 18종(🔥5) / 필드 이벤트 20종 / 업적·퀘스트·도감 완비
- **경제**: 골드 -75%, 도박장 코너(상자 400G/복권 200G/뽑기/따당), 강화 돌파식(+4~6 ★, +7~9 ★+◆), 승천석 = 레벨 6부터 3렙당 1개
- **비주얼(1단계)**: 피격 플래시, 빈사 연출, 악몽 도깨비불, 엘리트 룬 링, 보스 위압 링+페이즈 핍, 장비 5부위 외형(희귀도 색), 맵 13종 전용 BGM

### 남은 로드맵 (우선순위순) — ★가 사용자 최신 최우선 지시
1. ★ **풀 아트 개편 (비주얼 2단계)** — 사용자 원문 요지:
   - **모든 장비 하나하나 외형을 다 다르게** 구현 (아이템별 실루엣). 희귀도·유일무기(CGW)·성장무기는 특별히 멋있게
   - **성장무기 외형 진화**: 형편없는 모습 → 렙업할수록 그 어떤 장비보다 간지폭발 (v6.49에서 무명검 4단계 프롭으로 시작함 — PGW 111종·활/마도서/대검으로 확장 필요)
   - **전 직업 졸라맨 탈피**: 완전한 게임 수준의 캐릭터 디자인·비주얼 (Pixi 스프라이트 or 고급 캔버스 드로잉 — 직업별 체형/의상/무기 풀 렌더)
   - 보스도 그 캐릭터로 보이는 수준 (성기사=방패 휘두름 같은 실제 모션 포함 — v6.48 스윙은 1차)
   - 접근 제안: ① 장비 외형 프로시저럴 생성(슬롯×명사×희귀도 → 실루엣 조합) ② 직업 바디 템플릿 6계열 + 직업별 의상 레이어 ③ Pixi 이관은 몹→보스→플레이어 순
2. **전직 개성화 심화**: 현재 계열(6군)×길(3) 18종 각인 — 궁극적으로 직업별 전용 각인/모션 (37직업 단계적)
3. 리더보드/고스트 (사용자 보류 중 — 서버 필요)
4. 세부 폴리시: 남은 사운드, UI 다듬기

### 사용자 성향 (중요)
- 용어 정정에 민감: "직업 유일무기(CGW)" ≠ "성장무기(PGW·무명검류)" 혼동 금지
- 밸런스 훼손(파밍 악용 여지)에 민감 — 보상 신설 시 악용 경로 먼저 생각할 것
- "전부 전수조사해서 고쳐"를 자주 요구 — 한 곳 고치면 같은 패턴 전체를 찾아서 고칠 것
- 모바일 플레이 비중 높음 — UI 변경 시 반드시 좁은 화면·터치 기준 검토
- 실존 인물 보스는 커뮤니티식 코믹 개명(트황상, 마오쩌뚱, 시진핑핑이 등) 유지
